- CJ wants Codex available only as an intentional, dedicated coding lane, not as an automatic fallback for normal chat.
- OpenClaw config at ~/.openclaw/openclaw.json was updated to add a dedicated `codex-coding` agent using `openai/gpt-5.1-codex` with `textVerbosity: medium` to avoid Codex `text: low` request failures.
- A separate low-cost background agent `ops-lite` was added using `openai/gpt-4.1-nano`; it also needed `textVerbosity: medium` to avoid `text: low` incompatibility in this setup.
- Dedicated agent workspaces were created for `codex-coding` and `ops-lite`, and their bootstrap/prompt files were slimmed down to reduce per-turn token costs.
- Measured prompt-cost improvement: the Codex lane dropped from roughly 18.2k input tokens to about 11.2k on a tiny smoke test after slimming its workspace prompt files.
- Tool-surface trimming did not reduce injected tool schema as much as expected, so the confirmed cost wins came from model routing and smaller workspace prompt files.
- CJ is now explicitly interested in reducing OpenClaw costs while keeping main chat quality, so the recommended next optimization is moving heartbeat/background work onto `ops-lite` and then investigating remaining tool-schema bloat.
- CJ wants Codex available only as an intentional, dedicated coding lane, not as an automatic fallback for normal chat.
- OpenClaw config at ~/.openclaw/openclaw.json was updated to add a dedicated `codex-coding` agent using `openai/gpt-5.1-codex` with `textVerbosity: medium` to avoid Codex `text: low` request failures.
- A separate low-cost background agent `ops-lite` was added using `openai/gpt-4.1-nano`; it also needed `textVerbosity: medium` to avoid `text: low` incompatibility in this setup.
- Dedicated agent workspaces were created for `codex-coding` and `ops-lite`, and their bootstrap/prompt files were slimmed down to reduce per-turn token costs.
- Measured prompt-cost improvement: the Codex lane dropped from roughly 18.2k input tokens to about 11.2k on a tiny smoke test after slimming its workspace prompt files.
- Tool-surface trimming did not reduce injected tool schema as much as expected, so the confirmed cost wins came from model routing and smaller workspace prompt files.
- CJ is now explicitly interested in reducing OpenClaw costs while keeping main chat quality, so the recommended next optimization is moving heartbeat/background work onto `ops-lite` and then investigating remaining tool-schema bloat.
- New finding: setting per-agent `tools.profile` to `minimal` and using `alsoAllow` on the dedicated agents materially reduced built-in tool schema exposure.
- New finding: most remaining schema bloat in `codex-coding` came from the global Semrush MCP server defined in `mcp.servers.semrush`; per-agent tool filtering did not suppress that MCP tool namespace in local smoke tests.
- Additional prompt squeeze: setting `skills: []` for `codex-coding` and `ops-lite` removed skill-catalog prompt overhead; Codex smoke-test system prompt size dropped from about 20,002 chars to about 14,731 chars, with prompt tokens around 10.0k on the tiny test.
- Best next step for further prompt reduction is to remove or relocate the global Semrush MCP server into a dedicated context, since it appears effectively prompt-global in the current setup.
